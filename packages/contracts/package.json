{
  "name": "@atlas/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod schemas and domain types — the single contract between API, web and mobile",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
